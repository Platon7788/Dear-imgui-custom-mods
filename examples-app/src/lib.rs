//! Empty stub.
//!
//! `examples-app` has no library API; it exists as a Cargo package
//! purely so the 15 `examples/demo_*.rs` files have a manifest to
//! attach to. The published library lives in `../crate`.
